<div class= "container mb-5">
  <div class= "row">
    <div class= "col-lg-12">

      <%= @genre_1.name %><span class= "small">（全<%= @genre_1.products.count %>件）</span>
      <div class= "d-flex flex-wrap mb-5">
        <% @genre_1.products.each do |product| %>
          <div class= "product-box">
            <%= link_to product_path(product) do %>
              <%= attachment_image_tag(product, :image, size: "250x200", format: 'jpeg' ,fallback: "cake.jpg") %></br>
            <% end %>
            <%= link_to product.name, product_path(product) %></br>
            ¥ <%= product.price %>
          </div>
        <% end %>
      </div>
      <%= @genre_2.name %><span class= "small">（全<%= @genre_2.products.count %>件）</span>
      <div class= "d-flex flex-wrap mb-5">
        <% @genre_2.products.each do |product| %>
          <div class= "product-box">
            <%= link_to product_path(product) do %>
              <%= attachment_image_tag(product, :image, size: "250x200", format: 'jpeg' ,fallback: "cake.jpg") %></br>
            <% end %>
            <%= link_to product.name, product_path(product) %></br>
            ¥ <%= product.price %>
          </div>
        <% end %>
      </div>
      <%= @genre_3.name %><span class= "small">（全<%= @genre_3.products.count %>件）</span>
      <div class= "d-flex flex-wrap mb-5">
        <% @genre_3.products.each do |product| %>
          <div class= "product-box">
            <%= link_to product_path(product) do %>
              <%= attachment_image_tag(product, :image, size: "250x200", format: 'jpeg' ,fallback: "cake.jpg") %></br>
            <% end %>
            <%= link_to product.name, product_path(product) %></br>
            ¥ <%= product.price %>
          </div>
        <% end %>
      </div>
      <%= @genre_4.name %><span class= "small">（全<%= @genre_4.products.count %>件）</span>
      <div class= "d-flex flex-wrap mb-5">
        <% @genre_4.products.each do |product| %>
          <div class= "product-box">
            <%= link_to product_path(product) do %>
              <%= attachment_image_tag(product, :image, size: "250x200", format: 'jpeg' ,fallback: "cake.jpg") %></br>
            <% end %>
            <%= link_to product.name, product_path(product) %></br>
            ¥ <%= product.price %>
          </div>
        <% end %>
      </div>

    </div>
  </div>
</div>
